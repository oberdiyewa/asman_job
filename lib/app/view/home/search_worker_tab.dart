import 'package:flutter/material.dart';

class SearchWorkerTab extends StatefulWidget {
  const SearchWorkerTab({super.key});

  @override
  State<SearchWorkerTab> createState() => _SearchWorkerTabState();
}

class _SearchWorkerTabState extends State<SearchWorkerTab> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('2'),
    );
  }
}
