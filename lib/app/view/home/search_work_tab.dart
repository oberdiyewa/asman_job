import 'package:flutter/material.dart';

class SearchWorkTab extends StatefulWidget {
  const SearchWorkTab({super.key});

  @override
  State<SearchWorkTab> createState() => _SearchWorkTabState();
}

class _SearchWorkTabState extends State<SearchWorkTab> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('1'),
    );
  }
}
