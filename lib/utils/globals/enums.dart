enum EnumStorageKeys {
  none,
  isDarkMode,
  userToken,
}

enum EnumAuthenticationStatus {
  unknown,
  authenticated,
  unauthenticated,
}

enum EnumScreenName {
  home,
  notifs,
  profile,
  favorites,
}
